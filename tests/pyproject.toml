[tool.poetry]
name = "p3lib"
version = "1.1.107"

[tool.poetry.dependencies]
python = "^3.10"
click = "*"
requests = "*"
nicegui = "*"
plotly = "*"


[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"